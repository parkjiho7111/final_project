정책 모달 열릴 때 배경 마키 애니메이션 속도 느리게 (main.html만 수정)

현재 상태
마키 애니메이션: slide-row-1과 slide-row-2 요소에 scroll-left와 scroll-right 클래스가 적용되어 있음
애니메이션 속도: CSS에서 scroll-left는 40s, scroll-right는 45s로 설정됨
모달 요소: policy-modal ID를 가진 요소가 있고, 열릴 때 active 클래스가 추가되고 hidden 클래스가 제거됨
모달 닫기: hidden 클래스가 추가되고 active 클래스가 제거됨

구현 작업
main.html 수정

모달 상태 감지 및 마키 속도 제어
MutationObserver를 사용하여 policy-modal 요소의 클래스 변경을 감지
또는 주기적으로 모달 상태를 확인하여 마키 속도 조절
모달이 열릴 때 (active 클래스 추가 또는 hidden 클래스 제거):
slide-row-1과 slide-row-2 요소의 animation-duration을 느리게 변경 (예: 80s, 90s로 2배 느리게)
모달이 닫힐 때 (hidden 클래스 추가 또는 active 클래스 제거):
slide-row-1과 slide-row-2 요소의 animation-duration을 원래 값으로 복구 (40s, 45s)

구현 방법
main.html의 기존 스크립트 섹션에 마키 속도 제어 로직 추가
policy_modal.js가 로드된 후 실행되도록 DOMContentLoaded 이벤트에서 처리
수정할 파일
templates/main.html - 모달 상태 감지 및 마키 애니메이션 속도 제어 스크립트 추가

참고사항
policy_modal.js는 수정하지 않음

완료된작업
완료된 작업
모달 상태 감지
MutationObserver로 policy-modal 요소의 클래스 변경 감지
active 클래스 추가/제거와 hidden 클래스 변경 감지
마키 애니메이션 속도 제어
모달 열릴 때 (active 클래스 있고 hidden 클래스 없을 때):
slide-row-1: 40s → 80s (2배 느리게)
slide-row-2: 45s → 90s (2배 느리게)
모달 닫힐 때:
원래 속도로 복구 (40s, 45s)
구현 방식
element.style.animationDuration으로 직접 변경
DOMContentLoaded에서 초기화
policy_modal.js 로드 후 실행
정책 카드를 클릭해 모달이 열리면 배경의 정책 카드들이 지나가는 속도가 느려지고, 모달을 닫으면 원래 속도로 복구됩니다.
린터 오류는 Jinja2 템플릿 문법으로 인한 것으로, 실행에는 문제 없습니다. 구현 완료.
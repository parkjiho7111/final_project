마키 슬라이드 GSAP 리팩토링
main.html만 수정됨!!!

목표
templates/main.html의 마키 슬라이드 애니메이션을 CSS 기반에서 GSAP 기반으로 전환합니다. static/style.css는 다른 페이지에서도 사용되므로 수정하지 않고, main.html 스크립트에서 CSS 애니메이션을 비활성화한 후 GSAP로 구현합니다.

현재 상태
script.js (682-701줄): allSlideData를 복제하여 slide-row-1, slide-row-2에 렌더링
main.html (584-636줄): CSS animationDuration을 변경하여 모달 시 속도 제어
style.css: .scroll-left, .scroll-right 클래스에 CSS 애니메이션 정의 (수정 불가)

수정 계획
1. main.html 스크립트 교체
위치: templates/main.html 584-636줄

기존 코드 제거:

CSS animationDuration 변경 로직 (584-636줄)
새 코드 추가:

CSS 애니메이션 비활성화: row1.style.animation = 'none', row2.style.animation = 'none'
컨텐츠 복제: duplicateContent 함수로 innerHTML += innerHTML 실행
GSAP 애니메이션:
row1: xPercent: -50 방향, duration: 60, repeat: -1, ease: "none"
row2: 초기값 xPercent: -50, 목표값 xPercent: 0, duration: 65, repeat: -1, ease: "none"
모달 연동: MutationObserver로 모달 active 클래스 감지
열림: timeScale: 0.1, duration: 1.5
닫힘: timeScale: 1, duration: 1
2. 구현 세부사항
컨텐츠 복제 타이밍:

script.js에서 렌더링 완료 후 실행되도록 setInterval로 체크
각 행(row1, row2)의 children.length > 0 확인 후 복제
GSAP Tween 저장:

tween1, tween2 변수에 저장하여 모달 제어 시 재사용
에러 처리:

row1, row2, modal 요소 존재 여부 확인
GSAP 라이브러리 로드 여부 확인 (typeof gsap !== 'undefined')
파일 수정 목록
templates/main.html: 584-636줄 스크립트 교체
주의사항
style.css는 절대 수정하지 않음
script.js의 렌더링 로직은 그대로 유지
기존 모달 감지 로직과 충돌하지 않도록 주의